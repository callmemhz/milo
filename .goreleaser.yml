version: 2
project_name: milo

before:
  hooks:
    - go mod tidy

builds:
  - id: cli
    main: ./cmd/milo
    binary: milo
    env: [CGO_ENABLED=0]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/callmemhz/milo/internal/cli.Version={{.Version}}
      - -X github.com/callmemhz/milo/internal/cli.Commit={{.ShortCommit}}

archives:
  - id: cli-archive
    builds: [cli]
    name_template: "milo_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format: tar.gz
    files:
      - LICENSE
      - README.md


checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot+{{.ShortCommit}}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^chore:"
      - "^docs:"
      - "^test:"
      - "Merge pull request"
      - "Merge branch"

brews:
  - name: milo
    repository:
      owner: callmemhz
      name: homebrew-milo
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/callmemhz/milo"
    description: "milo CLI — single-host PaaS client"
    license: "Apache-2.0"
    install: |
      bin.install "milo"
    test: |
      system "#{bin}/milo", "version"
    # Skip pre-release tags
    skip_upload: auto
