services:
  milo-control-plane:
    image: ghcr.io/callmemhz/milod:${MILO_VERSION:-latest}
    container_name: milo-control-plane
    restart: unless-stopped
    environment:
      ROOT_DOMAIN: ${ROOT_DOMAIN}
      API_DOMAIN: ${API_DOMAIN}
      STATE_DIR: /var/lib/milo
      MILO_NETWORK: ${MILO_NETWORK:-milo-net}
      GHCR_USER: ${GHCR_USER}
      GHCR_TOKEN: ${GHCR_TOKEN}
      MILO_VERSION: ${MILO_VERSION:-latest}
    volumes:
      - milo-state:/var/lib/milo
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - milo-net

  caddy:
    # Custom Caddy build with the caddy-dns/alidns plugin baked in (xcaddy).
    # Swap Dockerfile.caddy if you want a different DNS provider.
    build:
      context: .
      dockerfile: Dockerfile.caddy
    image: milo-caddy:local
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      ALIDNS_ACCESS_KEY_ID: ${ALIDNS_ACCESS_KEY_ID}
      ALIDNS_ACCESS_KEY_SECRET: ${ALIDNS_ACCESS_KEY_SECRET}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - milo-net

volumes:
  milo-state:
  caddy-data:
  caddy-config:

networks:
  milo-net:
    driver: bridge
    name: ${MILO_NETWORK:-milo-net}
