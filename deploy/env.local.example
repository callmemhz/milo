# Local-testing environment. Copy to .env in this directory.
ROOT_DOMAIN=app.test
API_DOMAIN=milo.test

# GHCR creds optional for local testing with public images (e.g. nginx:alpine).
# Leave empty to skip registry auth.
GHCR_USER=
GHCR_TOKEN=
